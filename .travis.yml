language: php
php:
  - 5.4
  - 5.5
before_script:
  - curl -s http://getcomposer.org/installer | php
  - php composer.phar install --dev
script:
  - phpunit --coverage-text
notifications:
  irc:
    channels:
      - "chat.freenode.net#DwNotification2010"
    use_notice: true
